# Session 2026-08-27

## What was worked on

Gaurav approved GST DRC-01C as the active problem and mechanism, replacing the Cyber Crime direction after weeks of discovery work on it. Kriti has not yet confirmed the exact citizen journey. This session covered two things: drafting Codex-facing prompts for the remaining build stages, and (in parallel, run by Gaurav directly) Codex executing Stages 1–3 while keeping the flow provisional.

**Claude's work this session:**
- Dispatched a Fable 5 agent to draft a stage-1 "seal the PRD" prompt for Codex, built on the GST secondary research already in `research/GST - Secondary.md` and the `/prd-create` skill's stage structure.
- Gaurav caught a real bug in that first draft: it told Codex to "run `/prd-create`," a Claude Code skill invocation that means nothing to Codex, which has no skill system. Read the actual skill definition and rewrote the prompt with every stage, gate, and check spelled out in plain instructions.
- Read `AGENTS.md`, `PROJECT_CONTEXT.md`, `STATUS.md`, `IMPLEMENTATION_PLAN.md`, and `index.html`, and discovered Codex had already done a full-corpus GST analysis in an earlier session (2026-08-26): a four-tab decision brief (`index.html`), a comparison wireframe (`GST_WIREFRAME.html`), and a detailed `IMPLEMENTATION_PLAN.md`, all recommending the same DRC-01C notice-decoder direction, gated on team approval. Flagged this to Gaurav before drafting anything further, since the original plan (Codex starts research from scratch) would have duplicated real work.
- After Gaurav reviewed and approved the direction (Kriti still pending), wrote `Documentation/process/PRD_QUALITY_BAR.md` (the PRD structure, confidence-tag convention, 5 exit checks, and 10 judging-criteria gaps, as a standalone reference file, per Gaurav's instruction to never reference skill names directly in a Codex prompt) and `CODEX_PROMPTS.md` (all four stage prompts in one file, each stopping and reporting back before the next stage, with the flow-decision uncertainty from tonight's Kriti call built into Stage 1's PRD wording and Stage 2/3's flow-agnostic-vs-flow-specific split).

**What happened outside this session (Gaurav running Codex directly, reflected in the current file state):**
- Stage 1: `PRD.md` sealed for GST (archived the old Cyber Crime PRD to `Documentation/archive/PRD-cybercrime.md` first), all 5 exit checks run, all 10 judging-criteria gaps closed. Part B's four-view citizen-flow section is explicitly marked provisional pending tonight's Kriti + Gaurav call.
- Stage 2: `IMPLEMENTATION_PLAN.md` reconciled against the sealed PRD, cross-references updated, flow-agnostic work (fixtures, `reconcile()`, the OpenAI contract) separated from flow-specific view-building work.
- Stage 3: a flow-review skeleton scaffolded in `prototype/` (React/Vite/TypeScript/Vitest, per the plan's layout) and deployed to https://prototype-blue-three.vercel.app. The root `index.html` decision brief was preserved. The deterministic core proves the ₹18,400 + ₹3,200 = ₹21,600 reconciliation with focused tests; the four-view shell has no OpenAI call, print styles, or final visual design yet, intentionally deferred to Stage 4.

## Block 1 implementation details

- Project root: `prototype/`; Vercel deploys this directory rather than replacing the repository-root decision brief.
- Domain model: `prototype/src/domain/types.ts` defines `DemoCase`, `PurchaseClaimRow`, `Gstr2bRow`, the two allowed finding types, `Finding`, `ReviewBrief`, and `ReconciliationResult` using `DEMO-` template-literal identifiers.
- Fixture: `prototype/src/data/demoCase.ts` contains `DEMO-CASE-01`, April 2026 claimed ITC of ₹2,48,400, available ITC of ₹2,26,800, current-period matched rows, a ₹18,400 row appearing in May GSTR-2B, and a duplicated ₹3,200 workpaper key.
- Rules: `prototype/src/domain/reconcile.ts` validates every visible identifier prefix, checks workpaper and current-period totals, identifies later-period evidence and the probable duplicate, excludes clean matches, and refuses a fixture whose findings do not exactly explain the notice mismatch.
- Tests: `prototype/tests/reconcile.test.ts` contains six passing tests: aggregate arithmetic, later-period finding and both source IDs, duplicate finding and both workpaper IDs, clean-match exclusion, prefix coverage, and rejection of a non-`DEMO-` identifier.
- Shell: four navigable views—notice, evidence, explanation-stage review, and review brief—render actual fixture/reconciliation data. The explanation view states that no model call exists; the review view states that generated draft text and print treatment are deferred and that nothing was submitted to GSTN.
- Dependencies installed with zero reported npm vulnerabilities at install time. The production build completed successfully with Vite.

## Verification performed

- `npm test`: 1 test file passed, 6 tests passed.
- `npm run build`: TypeScript check and Vite production build passed.
- Local browser click-through reached all four views and confirmed the exact amounts and source IDs.
- At a 375px viewport there was no horizontal overflow and every button was at least 44px high.
- No browser warnings or errors were reported locally.
- The public alias https://prototype-blue-three.vercel.app loaded with the expected title, notice amount, and explicit `No OpenAI call in this build` disclosure; no browser warnings or errors were reported.

## Decisions made this session

- Gaurav approved GST DRC-01C as the platform, problem, reconciliation mechanism, and deterministic/OpenAI responsibility boundary, replacing Cyber Crime. Kriti's exact-flow confirmation remains open. See `memory/decisions.md`.
- Codex prompts must never reference Claude Code skill names; any process mechanics a prompt depends on get written into a separate reference file the prompt points to instead.
- Block 1 may be implemented before flow confirmation because its domain core is flow-agnostic; Blocks 2–4 may not continue until the team reports the confirmed flow.
- The deployed URL is a flow-review skeleton, not a submission candidate.

## What surprised me / what patterns emerged

- The biggest risk this session wasn't in anything Claude wrote, it was almost handing Gaurav a prompt full of dead references (`/prd-create`, `grill-me`, a skill-based interview script) that would have silently done nothing in Codex. Codex doesn't error on an unknown instruction, it just guesses, which is worse than a hard failure because the gap doesn't surface until much later.
- Discovering that Codex had already done a full research-and-recommendation pass in a prior session, invisible from this session's starting context, is exactly why `STATUS.md`/`AGENTS.md`/`PROJECT_CONTEXT.md` exist as a handoff mechanism. Reading them before drafting anything caught the duplication before it happened.
- The team is running a genuinely parallel workflow now: Claude drafts the process and prompts, Gaurav executes them directly in Codex outside the Claude session, and Claude picks the state back up cold. This session's `/end-session` had to reconstruct three stages of work from file diffs rather than from conversation turns.

## Lessons learned

See `memory/past-mistakes.md` for the skill-name lesson.

## Session-end gate

The session ends after Stage 3. Do not proceed to the full build or Stage 4 until Gaurav reports the confirmed flow decision from the 2026-08-27 call. If the flow changed, update `PRD.md` Part B and `IMPLEMENTATION_PLAN.md` section 6 before writing more view code. The qualified GST-practitioner review remains the next domain-safety gate before citizen-facing wording is frozen.

## Files created or materially updated across the session

- `PRD.md` — sealed GST PRD.
- `Documentation/archive/PRD-cybercrime.md` — preserved prior discovery PRD.
- `IMPLEMENTATION_PLAN.md` — reconciled to the sealed PRD and updated with the completed Block 1 exit.
- `prototype/` — Block 1 application, tests, build config, and Vercel configuration.
- `STATUS.md` — current URL, completed checks, blockers, and explicit session stop.
- `PROJECT_CONTEXT.md` — replaced the stale pending-Cyber-Crime state with the approved GST direction and deployed-skeleton boundary.
- `CODEX_PROMPTS.md` — four-stage execution prompts; Stages 1–3 are complete and Stage 4 remains gated.
- `Documentation/process/PRD_QUALITY_BAR.md` — durable PRD conventions, exit checks, and judging-gap list.
- `memory/decisions.md`, `memory/project.md`, `memory/MEMORY.md`, and this file — durable handoff trail.

## Repository-state caution

The worktree contained modified and untracked files from earlier sessions before this work began. No unrelated changes were reset, cleaned, staged, or committed.
