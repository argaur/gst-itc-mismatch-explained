# Session: 2026-08-26 (3rd session)

## What was worked on

1. **Re-verified the brief.** Gaurav asked for a fresh pass over buildwhatmovesindia.com/brief specifically to identify which sections are ranking-relevant, since the team risked over-building things that don't score. Dispatched a Fable 5 agent (per the standing model-routing rule) with a compiled prompt-compile brief. It fetched the live page (WebFetch got a 403, firecrawl_scrape succeeded), confirmed all 8 sections via the page's own "On this page" nav, and returned a full section-by-section extraction plus a gap check against PRD.md.

2. **Findings saved to project memory** (not just chat): `brief-judging-criteria.md` (the extraction, the 6-criterion rubric, submission mechanics) and later `codex-handoff-workflow.md` (the workflow decision, see below). One live tension flagged: CLAUDE.md's headline claims a fixed 10-platform list, but the live brief page itself still says "IRCTC, EPFO and the Income Tax portal are examples, not a fixed list." Not fixed this session — CLAUDE.md's own later paragraph already carries the accurate nuance, just flagged as inconsistent within the same file.

3. **Workflow change.** Gaurav decided that from now on, Codex builds this project directly and Claude's role narrows to planning and occasional guidance. He asked for three files: Codex instructions, project context, and a status file Claude can read for guidance later.

4. **Researched before building, rather than guessing.** Confirmed via web search that Codex CLI has a real, standard convention for this: it auto-loads `AGENTS.md` at the repo root every session, the same way Claude Code auto-loads `CLAUDE.md`. Used that instead of inventing a generic instructions filename Gaurav would have to remind Codex to read every time.

5. **Built and committed the handoff** (commit `95a28a8`): `AGENTS.md` (hard rules, the 6 judging criteria as a build checklist, quality bar, Codex-usage requirement), `PROJECT_CONTEXT.md` (full brief extraction, team/deadline info, judging checklist, problem direction explicitly marked PENDING RETHINK so Codex won't build against a direction that might change), `STATUS.md` (living status doc, the one file Claude should read first if pulled back in).

6. **Codex plan advice.** Researched ChatGPT Go vs Plus vs Pro for Codex CLI usage (web search, since this wasn't something to guess from training knowledge given it's pricing/limits as of Aug 2026). Go only unlocks the lighter Luna model tier for Codex with a smaller allowance and no credit top-ups. Plus unlocks the full model set and enough headroom for a paced 2-day sprint, but has a rolling 5-hour usage limit reintroduced Aug 26, 2026. Pro removes that limit but costs 5x-20x more, not justified for a 2-day hackathon. Recommended Plus, confirmed with Gaurav.

## Decisions made this session

- Codex builds, Claude plans, from 2026-08-26 forward — see `memory/decisions.md`.
- ChatGPT Plus over Pro for the Codex sprint.

## What surprised me / patterns worth naming

- The brief-re-verification found real, non-trivial gaps in the drafted PRD that hadn't surfaced in three prior sessions of research: no Codex/OpenAI-model usage plan at all, despite it being a hard eligibility rule, not a nice-to-have. Worth remembering: re-reading the primary source late in a project, after a lot of derived research has piled up, still catches things the derived research missed.
- Gaurav's rethink from the previous session still hasn't arrived — two sessions running now where the immediate next step is "hear Gaurav's rethink." Not a problem, just worth naming so a future session doesn't assume it happened.

## Lessons learned

None that rise to a past-mistakes.md entry — no genuine mistake this session, just a workflow decision and a research task done carefully (verify-before-recommend on the AGENTS.md convention and the Codex pricing tiers, rather than guessing either).
